class Player {
  constructor(name, token, wins) {
    this.name = name;
    this.token = token;
    this.challenger = []
    this.wins = wins;
  }

  takeTurn() {
    //if its not the current players turn it the other players turn
    playerChoice = this.challenger
  }
}
